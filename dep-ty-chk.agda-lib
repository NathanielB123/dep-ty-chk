-- We use a slightly customised version of 1lab with every module scoped under
-- an outer "1Lab" in order to avoid ambiguity conflicts with identically named
-- modules in the Agda std-lib
depend: standard-library, 1lab
include: .
flags:
  --no-load-primitives
  --rewriting
  --guardedness
  --cubical